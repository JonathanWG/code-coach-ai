version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
    command: gunicorn --bind 0.0.0.0:5000 src.entrypoints.web.app:app